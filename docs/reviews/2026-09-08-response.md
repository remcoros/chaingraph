# Independent release review response

A fresh Codex CLI session reviewed commit `37ddadf` through Herdr. It used `--ignore-user-config`, `--ephemeral`, disabled memories/plugins/skill search/apps/multi-agent, enabled skipping host skill discovery, disabled bundled skills and skill instructions, and set project instruction loading to zero. No conversation was forked. The reviewer confirmed that no skills, memory, other agents, user configuration or AGENTS.md files were used. Its complete findings are in [the independent report](2026-09-08-independent.md).

The review was of the prior committed baseline while implementation proceeded separately. These are the implemented responses to that report; final checks are recorded in [validation](../validation.md).

| Finding | Response |
| --- | --- |
| 1. Long imported wallet labels break reopening | Wallet names now accept the same 200-character bound as BIP329 labels, preserving previously saved 101–200-character names without truncation. Blank label imports keep required wallet names. Full workspace validation precedes replacing an encrypted save or exporting. |
| 2. Wallet addresses not bound to key | Imports and unlock derive every supplied address from its account key/script/branch/index and compare it, including sparse indexes without scanning gaps. |
| 3. Annotation draft remounted by view changes | Editor identity now depends only on workspace/entity. Dirty drafts survive view/scan changes, and actual conflicting saved edits offer explicit reconciliation. |
| 4. Scan overwrites newer label | Scan completion merges only scan-owned fields into the current wallet. |
| 5. Equal-output highlight includes unequal values | Analysis now records actual equal-value groups and their member outputs. |
| 6. Rerun restores excluded findings | Stable finding identity and unchanged node/transaction evidence preserve exclusion decisions. |
| 7. Finding focus does not move camera | Explicit selection centering moves the camera; finding isolation shows evidence with connected context and a reset. |
| 8. Entity list capped at 200 | Accessible pagination exposes every filtered entity, with sorting and total counts. |
| 9. Unlock privacy copy contradicts public names | Copy distinguishes public workspace names from encrypted descriptions, wallet names and contents. |
| 10. No deletion/quota recovery | Locked browser copies can be deliberately deleted with confirmation and cross-tab storage protection. |
| 11. Filters do not affect graph | Shared filters now determine both entity matches and rendered graph; context and hidden counts are explicit. |
| 12. Reproducible distribution missing | Version 0.2.0, pinned multi-stage Docker build, hardened Compose setup and tag-driven GHCR release workflow. |
| 13. Production rendering absent from CI | A real production-server browser smoke covers CSP/WebGL, annotation, encrypted save and reload/unlock, including the release container. |

A separate implementation review used [Vercel's Web Interface Guidelines](https://github.com/vercel-labs/web-interface-guidelines), consulted 2026-09-08, to check accessible controls, focus, content overflow and navigation. That guidance was not available to the independent reviewer. The project-local UI checklist preserves those practical checks for future work.

Follow-up implementation review also corrected history references after graph removal, preserved the selected path depth during navigation, cleared pending lookup/focus state on workspace changes, guarded tour storage failures and prevented cancelled operations from posting into another workspace. Browser review fixed mobile pagination clipping and added direct navigation between tools and findings.
